# UV

[project]
name = "schema205"
version = "2.0.0"
description = ""
authors = ["Open205"]
requires-python = ">= 3.10"

dependencies = [
  "jsonschema",
  "pyyaml",
  "Jinja2 >= 3.1.4",
  "lattice"
]

[tool.uv]
dev-dependencies = [
    "pytest >= 7.1.3",
    "doit",
    "pylint",
    "tomli == 2.0.1"
]

[tool.uv.sources]
lattice = {git = "https://github.com/bigladder/lattice.git"}
