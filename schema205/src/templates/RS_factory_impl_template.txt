#include "{{ factory_subclass }}.h"
#include "{{ subclass }}.h"
#include <memory>

/// @note  This class has been generated from a template. Local changes will not be saved!

using namespace {{container}};

std::unique_ptr<{{ base_class_root_name }}_base> {{ factory_subclass }}::Create_instance(const char* RS_instance_file) const
{
    auto p_rs = std::make_unique<{{ subclass }}_NS::{{ subclass }}>();
    auto j = tk205::Load_json(RS_instance_file);
    if (j["metadata"]["schema"] == "{{ subclass }}")
    {
        p_rs->Initialize(j);
    }
    return p_rs;
}
